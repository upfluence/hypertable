{{#if this.loading}}
  <OSS::Skeleton @height={{10}} @width="100%" />
{{else if (has-block)}}
  {{yield}}
{{else}}
  {{#if this.loading}}
    <OSS::Skeleton @height={{10}} @width="100%" />
  {{else if (has-block)}}
    {{yield}}
  {{else}}
    {{component this.renderingComponent item=this.item column=this.column manager=this.manager}}
  {{/if}}
{{/if}}