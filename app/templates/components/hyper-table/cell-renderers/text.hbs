{{#if isSuccess}}
  {{log 'isSuccess' isSuccess}}
{{/if}}

{{#if isEditing}}
  {{log 'isEditing' isEditing}}
{{/if}}

{{#if isSaving}}
  {{log 'isSaving' isSaving}}
{{/if}}

{{#if isError}}
  {{log 'isError' isError}}
{{/if}}

{{#if (and emptyValue (or (not editStatus.status) isSuccess))}}
  {{#if column.upsertable}}
    Add Value
  {{else}}
    —
  {{/if}}
{{else}}
  <div class="editing-input">
    {{#if (and editStatus.status (not isSuccess))}}
      <div class={{if isError 'is-invalid'}}>
        {{input
          type="text" value=value class="editing-input__field form-control"
          focus-out=(action "toggleEditing" value)
          insert-newline=(action "toggleEditing" value) }}
      </div>
    {{else}}
      <div data-toggle="tooltip" data-title={{value}} data-placement="bottom">
        {{value}}
      </div>
    {{/if}}
  </div>
{{/if}}

{{!-- TODO: Extract to component --}}
{{#if column.upsertable}}
  {{#if isSuccess}}
    <span class="text-color-success upf-icon upf-icon--check padding-left-xx-sm"></span>
  {{else if (or isEditing isError)}}
    <i class="fa fa-save padding-left-xx-sm"></i>
  {{else if isSaving}}
    {{loading-state}}
  {{else}}
    <i class="fa fa-pencil padding-left-xx-sm" onclick={{action "toggleEditing"}}></i>
  {{/if}}
{{/if}}
