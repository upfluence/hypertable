{{#if emptyValue}}
  —
{{else}}
  <div class='list-container' {{action 'toggleList' bubbles=false}}>
    {{#if value.firstObject.name}}
      <div>
        {{value.firstObject.name}}
      </div>
      {{#if (eq manager.tetherOn elementId)}}
        <div class='expandable-list'>
          {{#each value as |item|}}
            <div class='expandable-list__item' {{action 'goToUrl' (concat url item.id) bubbles=false}}>
              {{item.name}}
            </div>
          {{/each}}
        </div>
      {{/if}}
    {{else}}
      <div {{enable-tooltip title=(get value "0") html=true placement="bottom"}}>
        {{get value '0'}}
      </div>
    {{/if}}

    <div class='margin-left-xxx-sm list-elements' {{enable-tooltip title=formattedList placement="bottom" html=true}}>
      {{#if length}}
        +{{length}}
        more
      {{/if}}
      <i class='fa {{if (eq manager.tetherOn elementId) 'fa-caret-up' 'fa-caret-down'}} padding-left-xxx-sm'></i>
    </div>
  </div>
{{/if}}
