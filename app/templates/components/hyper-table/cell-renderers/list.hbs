{{#if emptyValue}}
  —
{{else}}
  <div class="list-container">
    <div data-toggle="tooltip" data-html="true" data-title={{get value '0'}} data-placement="bottom">
      {{get value '0'}}
    </div>
    {{#if length}}
      <div class="text-color-default-lighter margin-left-xxx-sm list-elements" data-toggle="tooltip" data-html="true" data-title={{formattedList}} data-placement="bottom">
        +{{length}} more
      </div>
    {{/if}}
  </div>
{{/if}}
